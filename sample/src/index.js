import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';


ReactDOM.render(
  <App />,         //app function in App.js which returns some value
  document.getElementById('root')
);
